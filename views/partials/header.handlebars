<header>
      <!-- Fixed navbar -->
      <nav class="navbar navbar-expand-md navbar-dark fixed-top">
        <a class="navbar-brand" href="/profile">Building Sports Network</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse text-center" id="navbarCollapse">
            {{#if user}}
                <ul class="navbar-nav">
                    <li class="nav-item btn-sm">
                        <a class="nav-link" href="/profile">Profile</a>
                    </li>&nbsp
                    <li class="nav-item btn-sm">
                        <a class="nav-link" href="/dashboard">Dashboard</a>
                    </li>&nbsp
                    
                    <li class="nav-item btn-sm">
                        <a class="nav-link" href="/teams">Clubs</a>
                    </li>&nbsp
                    <li class="nav-item btn-sm">
                        <a class="nav-link" href="/tournaments">Tournaments</a>
                    </li>&nbsp
                    {{!-- Sign in as organizer --}}
                    {{#if user.user.organizer}}
                        <li class="nav-item btn-sm btn-primary">
                            <a class="nav-link" href="/createTournament">Create Tournament</a>
                        </li>&nbsp
                    {{/if}}
                </ul>
            {{/if}}
                
            <ul class="navbar-nav ml-auto">
                {{#if user}}
                    <li class="navbar-nav btn-sm">
                        <a href="/auth/logout" class="nav-link">Logout</a>
                    </li>&nbsp
                {{else}}
                    <li class="nav-item btn-sm">
                        <a href="/auth/login" class="nav-link">Login</a>
                    </li>
                {{/if}}
            </ul>
        </div>
      </nav>
    </header>